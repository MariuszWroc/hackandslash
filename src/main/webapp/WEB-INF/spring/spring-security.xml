<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-4.0.xsd">

	<http pattern="/resources/css/**" security="none" />
	<http use-expressions="true" disable-url-rewriting="true">
		<access-denied-handler error-page="/403" />
		<intercept-url pattern="/" method="GET" access="permitAll"/>
		<intercept-url pattern="/admin/**" access="hasRole('ROLE_ADMIN')" />
		<intercept-url pattern="/user/**"
			access="hasAnyRole('ROLE_USER', 'ROLE_ADMIN')" />
		<form-login 
			login-page="/loginAdmin" 
			login-processing-url="/j_spring_security_check"
			default-target-url="/admin/panel"
			authentication-failure-url="/failed" 
			username-parameter="username"
			password-parameter="password" />
		<logout delete-cookies="true" logout-success-url="/"
			invalidate-session="true" />
		<!-- disable csrf protection -->
		<!--<csrf disabled="true"/> -->
	</http>
	
	<authentication-manager>
		<authentication-provider>
			<!-- <jdbc-user-service data-source-ref="DataSource" users-by-username-query=" 
				SELECT login, password, CASE enabled WHEN 1 THEN 'true' ELSE 'false' END 
				'enabled' FROM user WHERE login=?;" authorities-by-username-query=" SELECT 
				u.login, r.rolename FROM user u, role r WHERE r.id = u.role_idRole AND u.login=?;" 
				/> 
-->
                    <user-service>
                        <user name="mczarny" password="123" authorities="ROLE_USER" />
                        <user name="admin" password="admin" authorities="ROLE_ADMIN" />
                    </user-service>
                </authentication-provider>
            </authentication-manager>
</beans:beans>